# v3.0.11

## 版本概览

修复素材页首次进入时部分列表闪现和卡片布局问题，完善默认分组选择及同组多个 Key 的复用逻辑。

## 新增功能

- 默认密钥分组选项读取上游数据库，支持刷新、单项清空及全部清空。

## 功能改进

- 按当前用户和分组复用可用 Key，仅在没有可用 Key 且通过账号密码登录时创建；多个分类共用同一分组时避免重复创建。
- 同组多个 Key 按模型支持情况参与任务轮询，并保留用户已明确选择或清空的偏好。
- 素材页每页前六张预览立即加载，其余预览继续按需加载。

## 问题修复

- 修复素材接口先后返回导致首屏先显示少量素材、随后总数及内容跳变的问题；首次同步结束后统一展示列表和分页。
- 修复素材卡片标签高度不一致、长模型名称换行及底部操作栏错位的问题。
- 修复未配置分组时显示 <nil> 的问题；同名 Key 存在歧义时提示重命名，避免错误复用或重复创建。
- 修正分组初始化客户端的包依赖，符合项目架构检查要求。

## 移除与调整

- 默认 Key 初始化改为按分组匹配，SSO 登录复用已有 Key；缺少创建凭据时提示用户处理。
