# v2.1.0

## 版本概览

本版本重点完善跨设备创作配置、工作流执行体验和服务端数据治理。创作参数、模型 Key 选择与自定义中继配置统一按账号持久化，工作流任务与创作台会话彻底隔离，同时补齐日志定时清理和多家视频服务的请求合同。

## 新增功能

- 新增账号级创作台参数持久化，图片与视频模型、尺寸、质量、数量及相关生成选项可跨浏览器和设备复用。
- 新增账号级模型 Key 选择持久化，图片、视频、文本和音频通道分别保存所选凭据，不再依赖浏览器本地存储。
- 新增自定义 API 配置，管理员可保存个人 Base URL 与 Key，并可通过站点设置决定是否向普通用户开放。
- 新增日志定时清理，可按服务器本地时间每天自动执行保留策略，并支持在设置页配置开关和执行小时。
- 工作流新增独立任务记录、运行状态筛选、完整任务详情、结果下载和多图提示词审核流程。

## 功能改进

- 工作流添加、编辑和运行统一复用创作台图片参数组件，模板参数彼此隔离，任务运行期间以只读方式展示保存的参数快照。
- 重构顶部任务队列为空状态和运行列表，工作流任务不再混入创作台会话或全局创作任务队列。
- 优化日志治理、工作流、个人配置、提示词来源、画布侧栏、素材选择和多处弹窗的布局、滚动与响应式表现。
- 统一全局交互控件的禁用状态，并修正复合尺寸输入、选择器、按钮和深色主题下的视觉反馈。
- 提示词来源拉取改为明确的手动操作，移除仅在页面打开期间生效的伪定时配置。
- 扩展 KIE、APIMart、Veo、Kling、Seedance 等视频模型的能力识别、参考素材限制和厂商参数适配。

## 问题修复

- 修复不同工作流模板共用参数引用，导致编辑一个模板时另一个模板同步变化的问题。
- 修复工作流启动任务后弹窗未关闭、任务结果无法查看、结果弹窗层级和下载位置不合理的问题。
- 修复工作流任务出现在创作台历史和全局任务队列中的数据串联问题。
- 修复跨设备登录后需要重新选择模型 Key，以及部分创作参数仍写入浏览器存储的问题。
- 修复父级 `fieldset` 禁用时 W/H 复合输入框出现黑灰双色、内部透明度重复叠加的问题。
- 修复视频厂商请求中的模型别名、尺寸、时长、分辨率、音频、首尾帧和参考素材组合映射错误。

## 移除与调整

- 移除工作流专用图片历史存储，工作流结果统一进入独立任务记录，不再创建创作台图片会话。
- 移除提示词来源的浏览器页面定时拉取配置及对应环境变量。
- 移除账号创作参数和模型 Key 选择对 localStorage 的依赖；浏览器仅保留当前创作会话和页面间提示词交接等临时状态。
