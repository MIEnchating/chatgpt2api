# v3.0.7

## 版本概览

完成全项目审查并修复 85 项问题，重点改善账号权限隔离、媒体隐私、存储一致性，以及创作台、画布和工作流的任务恢复与交互可靠性。逐项修复与验证边界见 `REVIEW.md`，594 个文件的审查清单见 `review/inventory.json`。

## 新增功能

- 无。

## 功能改进

- 完善回归验证：前端 773 项测试，以及后端全量测试、静态检查、并发检测、数据库集成和桌面/手机浏览器检查。
- 工作流恢复会继续发现后续子任务；部分提交失败时保留已有输出，并按恢复预算结束缺失任务的等待。
- 管理页面完善保存反馈、重复提交保护、日志配置保存入口和模型排序后的默认模型同步。

## 问题修复

- 修复多实例登录、退出、禁用与角色撤权之间的竞态，防止旧权限重新签发或覆盖已持久化的管理状态。
- 修复账号切换、页面卸载或草稿替换后，旧上传、批量提交、任务结果和管理操作继续影响新会话的问题。
- 加强图片代理的 SVG 脚本隔离，修复私有媒体共享缓存、日志及诊断 URL 凭据泄露，并对仓库截图与研究脚本捕获流程进行脱敏和凭据隔离。
- 修复 SQLite 重连参数丢失、外部数据库 Schema 误识别、上传取消未生效、文件误删及公开素材读取权限不一致的问题。
- 修复任务重载丢失选项、已删除任务重新出现或继续授予媒体权限，以及 SSE 完成后继续读取导致成功结果误报失败的问题。
- 修复画布复制后删除副本误删原图、归档遗漏媒体、批次结果恢复错位、15 张批次无法保存，以及旧任务污染当前画布的问题。
- 修复创作台模型与 Key 选择、参考素材规范化、音频时长竞态、视频取消补偿及无效尺寸校验问题。
- 修复导演台删除最后机位白屏和粘贴操作修改原机位目标，以及输入法、键盘操作、非法 URL fragment 和登录图片重试等交互问题。
- 修复包含美元符号的配置被 Compose 插值改写、数据库密码空白被裁剪及部分日志字段保存错误的问题。

## 移除与调整

- 用户显式配置的存储 Provider 仅接受 S3/WebDAV，不再允许指定服务器本地目录；系统本地存储仍由服务器管理。
- 密码保留首尾空格，新建或更新密码统一校验为 8–72 个 UTF-8 字节。
- 私有图片、参考图和缩略图使用禁止共享缓存的响应策略；本次修复无法追溯清除旧外部缓存、历史日志或历史 Git 提交中的副本。
- 本次未调整 ChatGPT 网页上游协议；真实上游与真实 S3/WebDAV 未联调，详细验证边界见审查记录。
