# v3.0.14

## 版本概览

完善无限画布的布局、节点操作和 Agent 工作流，新增 Codex/MCP 本地连接、Skill 管理、媒体编辑及 AutoDL/方舟视频协议支持，统一素材标签和文件回收，并提供 Windows 桌面端源码及手动构建流程。

## 新增功能

- 画布 Agent 支持 Codex 本地连接和外部 MCP 工具调用，提供连接插件源码；连接凭据仅保留在页面内存，外部写入操作需在画布页面确认。
- 新增个人与系统 Skill 管理、Markdown 导入和附件读取，支持会话选择、长对话摘要与画布节点分页检索。
- 新增视频首帧、当前帧、尾帧截图与音频分离，音频支持选段试听和截取，编辑结果生成独立节点。
- 新增 AutoDL ComfyUI 视频/音频工作流线路、动态参数表单与视频模型契约导入，以及火山方舟 Seedance 原生协议和 Agent Plan 基础路径支持。
- 新增 Windows 桌面端源码，支持本机空间与远程服务器、隔离会话和本地服务生命周期管理；提供手动构建安装包的 Actions 工作流，本次稳定发布仍提供 Linux 二进制与 Docker 镜像。

## 功能改进

- 统一画布顶部、侧栏、节点操作、运行设置与 Codex 连接面板的全局组件样式；固定底部添加节点工具栏位置，改善窄屏布局、按钮尺寸和弹层层级。
- 节点列表支持分组树与折叠，完善生成状态、节点定位、图片引用顺序和视频焦点下的画布操作。
- Agent 的 Chat/Responses 请求保留推理与工具调用回放，校验工具参数并处理截断输出；默认先建立节点，可通过运行设置开启自动生成媒体。
- 素材标签支持统一筛选和归属编辑；手填媒体 URL 可读取文件信息，Chat/Responses 图片编辑可按原顺序混用公网参考 URL 与上传文件。
- 统一存储文件采用持久回收队列，持续 24 小时无引用才删除；撤销与重做历史通过租约保护文件，扫描或存储删除失败时保留并重试。
- 优化缩略图内存使用、素材批量清理和画布父子关系检测，补充前后端、桌面端及本地 Agent 回归测试与验证文档。

## 问题修复

- 修复侧栏入口不明显、开关面板导致底部工具栏移动、按钮尺寸不一致，以及运行设置和顶部连接控件偏离全局样式的问题。
- 修复切换账号或工作区后旧异步请求覆盖当前状态、重复提交和保存期间草稿丢失的问题，完善管理页面、模型契约和工作流操作的并发隔离。
- 修复画布活动指针跨工作区复用、循环分组关系、撤销历史租约过期后仍恢复旧文件引用，以及生成完成后错误恢复旧历史的问题。
- 修复图片参考顺序丢失、视频取消选中后继续播放、素材信息请求覆盖新表单，以及无扩展名 WebDAV 视频文件类型不正确的问题。
- 修复已有登录会话仍触发 SSO、认证暂时失败无法重试、提示词来源禁用配置未生效，以及中文输入法确认时误提交的问题。
- 修复文件系统错误回显内部路径、引用操作取消后继续等待，以及研究脚本未正确拼接多行 SSE 数据的问题。

## 移除与调整

- 素材分组统一称为标签，历史分组与旧 tags 合并到同一归属关系，保留既有名称和成员。
- 创作聊天任务统一使用 `/api/creation-tasks/chat-completions`，由独立的请求与协议模块处理。
- 本地旧二进制备份加入忽略规则；运行数据、依赖及安装包不纳入源码提交。
