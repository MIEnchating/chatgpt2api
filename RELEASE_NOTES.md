# v3.0.13

## 版本概览

恢复 SSO 与账号密码登录并存，完善图片生成参数、自定义端点和上传限制，优化画布导入导出及长对话处理，并修复画布引用、全景生成和配置保存问题。

## 新增功能

- 无

## 功能改进

- 画布 ZIP 打包和解包移入 Web Worker，共享媒体去重并限制读写并发，减少大项目导入导出对界面响应的影响。
- 优化图片对话历史合并，避免逐条重复扫描已有消息。
- 优化密码登录并发，将密码计算移出全局认证锁，签发会话前重新校验账号状态。
- 画布图片上传统一执行请求体大小限制、并发控制和临时文件清理。

## 问题修复

- 修复开启 SSO 后无法使用用户名或邮箱密码登录、已有普通登录会话失效的问题；两种登录方式均保留默认密钥初始化，SSO 会话继续校验平台状态。
- 修复图片编辑上传丢失 API 模式、画布来源和会话标识，以及 Responses 和 Chat 图片生成丢失自定义端点的问题；结果下载保留对应的网络安全策略。
- 修复图片模型参数归一化后生成数量丢失、非法数量未在归一化前拒绝的问题。
- 修复画布导入导出时媒体文件名冲突、共享媒体重复上传和缩略图引用映射错误，并加强 ZIP 尺寸一致性校验。
- 修复全景批量生成部分失败或取消后节点状态未恢复、首个有效结果未同步至主节点的问题。
- 修复复制画布节点后提示词引用、视频首尾帧和声音克隆引用未重映射，以及自动视频时长无法复制的问题。
- 修复取消连线时仍创建连接、Agent 连线未校验节点类型、中文输入法回车误提交和忙碌时重复提交画布名称的问题。
- 修复画布素材选择器将视频封面作为视频播放的问题。
- 修复登录页图片小数设置保存失败、数据库 IPv6 地址拼接错误，以及视频规则归一化改变必选条件含义的问题。
- 修复删除图片对话后排队的保存请求一直等待的问题，并限制 DOCX 模型文档的累计解压大小。
- 修复画布 Agent 保存失败后回退并发编辑的问题，完善复制节点的分组引用和中文输入处理。
- 对日志中的网络错误链执行 URL 脱敏，避免记录地址中的凭据和签名参数。

## 移除与调整

- 移除启用 SSO 时强制关闭 New API 密码登录和普通会话的限制；SSO 自动创建密钥仍需平台提供对应接口及配置上游数据库连接。
