# v3.0.0

## 版本概览

本版本重构视频生成协议与模型配置体系，以可管理的视频模型契约替代写死的厂商适配逻辑，并集中完善创作台、无限画布、素材库、工作流和设置页面。数据库查询、审计日志、后端聚合接口及前端加载链路同步完成全局性能治理，生成参数、参考素材、任务状态和跨设备持久化现在使用一致的数据边界。

## 新增功能

- 新增视频模型契约管理，支持模型匹配、生成模式、素材限制、参数映射、传输方式、轮询状态和结果字段的声明式配置。
- 新增视频契约 JSON 导入导出、文档与网页分析、保存前校验、参数面板预览和高级条件规则编辑。
- 新增 Key 级自定义中转配置，管理员可控制普通用户是否允许添加自定义 Base URL 与密钥，并可按当前 Key 拉取可用模型。
- 新增全局 InputTag、多选控件、统一画幅组件和中文视频播放器，支持模型名称、画幅、时长与清晰度等可扩展选项。
- 新增会话首页窗口聚合接口、日志游标分页以及模型和创作偏好的短时请求缓存。

## 功能改进

- 日志列表、日期范围和治理统计使用专用索引与 SQL 聚合，媒体元数据、缩略图和素材可见范围改为批量读取，减少全表扫描和 N+1 查询。
- 审计日志停止记录媒体二进制、SSE 和高频成功读取正文，并限制超大响应捕获，显著降低数据库写入与存储占用。
- 上游数据库结构探测增加线程安全缓存，创作偏好、模型配置、会话窗口和提示词外部源合并重复请求并避免旧响应覆盖新配置。
- 无限画布支持同类型节点并行生成，画布任务统一进入任务队列；素材刷新、公告轮询和提示词分类加载改为可见性驱动与限量加载。
- 创作台、无限画布和工作流统一视频参数、参考素材预览、生成尺寸占位、模型记忆及结果存储逻辑。
- 优化全局弹窗、滚动区域、下拉选择、Tooltip、素材卡片、工作流编辑器和设置页面在亮暗主题下的布局与交互。
- 画布 Agent、素材选择器、图片工具和预览组件改为按需加载，降低无限画布首屏脚本体积。

## 问题修复

- 修复 MiniMax H3 等视频模型因错误发送 `generation_mode`、素材组合不合法或字段映射不一致导致的上游校验失败。
- 修复视频模型契约编辑黑屏、高级规则显示不完整、参考资料预览缺失和配置名称被误当作主键的问题。
- 修复创作台模型选择刷新后丢失、图片和视频占位比例错误、视频播放器控制条抖动及下载入口缺失的问题。
- 修复无限画布提示词过多时卡顿、任务只能串行、引用删除难以点击、Tooltip 闪烁和素材位置受文本域裁剪的问题。
- 修复素材库跨设备存储不一致、远程媒体受固定 Base URL 影响、公开范围操作和多选卡片交互不一致的问题。
- 修复弹窗关闭按钮与标题或滚动条重叠、页脚留白重复、打开预览时 Tooltip 残留以及设置页吸顶圆角漏出内容的问题。
- 修复下拉框再次点击不能关闭、自定义滚动区域手感卡顿、亮色节点列表阴影异常和多处全局间距不一致的问题。

## 移除与调整

- 移除写死的视频厂商适配器、旧视频能力 JSON 和 Kling 专用工作台，统一由视频模型契约驱动请求与结果解析。
- 移除无限画布参数栏中暂未使用的系统提示词和提示词库入口，保留直接提示词输入与节点引用。
- 移除高频只读接口的成功审计记录，写操作、失败请求和管理员敏感查询继续保留。
- 视频任务不再根据模型名称猜测上游协议，未配置或未启用匹配契约时会明确拒绝提交。
