# v2.0.0

## 版本概览

云棉升级为面向自托管场景的 AI 多媒体创作与管理应用。本版本统一创作台、无限画布、工作流、素材库和存储体系，并收紧为仅供登录后 Web 界面使用的内部创作服务。

## 新增功能

- 新增创意工作流，支持私有与公开模板、运行变量、参考图、运行快照、图片系列拆分审核、受控并发和 Agent 草稿生成。
- 无限画布新增文本生成、视频、音频、全景图、导演台和生成配置节点，并补齐节点连接、资源引用、批量结果与任务恢复。
- 新增画布 Agent，通过受限结构化工具创建、更新、连接、排列和生成节点，对话状态随画布项目保存。
- 新增提示词库和统一素材库，支持文本、图片、视频、音频的跨设备管理、公开可见性、多选下载和删除。
- 新增服务器本机、S3、Cloudflare R2 和 WebDAV 素材存储 Provider，支持管理员与个人配置、容量上限、真实容量统计和定时同步统计。
- 新增 3D 导演台，支持场景树、角色与模型、变换工具、多机位、全景环境、时间轴、关键帧、截图和 MP4 导出。

## 功能改进

- 对齐创作台、无限画布和工作流的图片、视频、音频与文本模型能力、默认参数、参考素材限制和请求归一化合同。
- 统一无限画布各节点的参数面板、资源引用、快捷操作、生成按钮、任务状态、撤销记录和结果布局。
- 全局页面使用完整应用宽度，统一卡片交互、表单状态、自定义滚动区域、弹窗、分页和亮暗主题表现。
- 重构设置、个人资料、媒体治理、存储配置、日志管理、提示词库、工作流和素材库的信息层级与响应式布局。
- 重组 Go 后端与 React 前端的职责边界，素材、工作流、任务、存储和厂商协议分别使用独立服务或适配器。

## 问题修复

- 修复画布任务在刷新、切换项目、批量部分成功、取消和持久化恢复时可能出现的结果覆盖、顺序错乱或状态回退。
- 修复远端存储媒体受固定 `IMAGE_BASE_URL` 影响而无法访问的问题，外部存储改用统一鉴权文件合同。
- 修复素材选择、卡片查看区域、分页位置、多选操作、滚动条和不同页面宽度不一致的问题。
- 修复视频模型参数、参考素材组合、画幅比例、时长、音频开关和厂商专用字段在提交边界上的错误映射。
- 修复日志筛选、详情分组、表格层级、分页和吸顶表头透明导致内容重叠的问题。

## 移除与调整

- 删除对外开放 API、个人 API Token 和请求头令牌鉴权；所有本地 `/v1/*` 路径固定返回 `404`，创作能力只通过登录后的同源 Web 界面使用。
- 不再读取 `CHATGPT2API_*`、`RELAY_*`、旧账号调度、`IMAGE_STORAGE_BACKEND` 和 `S3_*` 环境变量；升级前必须按 README 迁移到当前配置。
- 上游用户数据库固定使用 `DATABASE_*` 或 `DATABASE_URL`，业务数据库固定使用 `STORAGE_DATABASE_URL`，不再自动交换旧数据库用途。
- 素材媒体不再写入浏览器 IndexedDB，统一保存到服务器或已配置的 S3、R2、WebDAV；旧浏览器本地媒体不会自动迁移。
- 旧账号池、注册、邮件、CPA、ChatGPT 文本代理与相关兼容代码已经移除。
