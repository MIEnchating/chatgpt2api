# v2.2.0

## 版本概览

本版本集中完善工作流、无限画布、素材库和后台管理体验。工作流多图创作获得更清晰的分步引导与结果管理，管理页面统一布局和分页，画布兼容旧文档数据，同时补齐生成素材来源、任务历史清理和权限识别等关键链路。

## 新增功能

- 工作流多图创作新增分步引导、逐图提示词审核、批量生成进度和逐图结果查看。
- 工作流任务历史新增持久化清理，清除记录时可选择是否同步删除关联生成素材。
- 无限画布新增旧文档数据兼容，可规范化历史节点的生成类型并恢复可加载状态。
- 日志治理新增按日定时清理配置，可在管理界面设置启用状态、执行时间和保留策略。
- 生成图片新增来源元数据，素材库可区分创作台、工作流和无限画布产物。

## 功能改进

- 统一用户管理、角色权限、日志管理、素材库和工作流等页面的背景、内容高度、操作行与固定底部分页。
- 重构角色授权页面，按菜单与接口权限分组展示，补充权限检索、变更提示和未保存切换保护。
- 优化工作流模板列表、任务记录弹窗、多图提示词布局、结果卡片和下载操作位置。
- 工作流创作参数继续复用创作台参数组件，生成期间保持只读，并确保不同模板的参数状态相互隔离。
- 提取公共提示词文本域框架，工作流与无限画布复用一致的可滚动输入体验。
- 统一弹窗、滚动区域、交互卡片和全局禁用状态的视觉与交互反馈。

## 问题修复

- 修复工作流保存响应缺少空 `negative_prompt` 时，生成完成后调用 `trim()` 导致页面黑屏的问题。
- 修复工作流与无限画布生成图片在“我的素材”中统一误显示为“生成图片”的问题。
- 修复已授权用户加载提示词库时仍被判定为无权限的问题。
- 修复工作流任务混入创作台历史、模板参数联动、启动后弹窗未关闭及任务结果无法完整查看的问题。
- 修复无限画布历史文档因节点生成类型不兼容而报 `invalid canvas document` 或加载失败的问题。
- 修复管理页面背景、分页样式和底部留白不一致，以及部分禁用输入框出现黑灰双色的问题。

## 移除与调整

- 工作流运行结果不再使用易遮挡内容的全屏图片预览，统一在任务结果卡片和详情弹窗中查看。
- 工作流任务不再显示在创作台页面或全局创作队列中。
- 管理页面移除各自独立的分页和内容高度实现，统一使用共享管理页布局组件。
