# v3.0.12

## 版本概览

统一管理与创作界面，新增 SSO 分组 Key 自动创建及 Gemini 图片模型支持，并修复全仓库审查发现的素材、工作流、文件引用与并发问题，优化画布、图片上下文、日志查询和用量统计的效率。

## 新增功能

- New API SSO 登录后按文本、图片、视频和音频分组复用或自动创建默认 Key。
- 支持 `gemini-3.1-flash-image-preview` 的文生图、参考图编辑、分辨率和扩展宽高比，保持模型原名。

## 功能改进

- 统一登录、管理、创作台、画布和工作流的公共组件、浅深色主题、间距及中文字体，完善移动端、短屏、长文本和弹窗滚动体验。
- 完善键盘焦点、减少动态效果设置和中文日期选择器，按访问权限预加载导航目标页面。
- 将画布批次筛选降为线性复杂度，避免任务队列状态未变化时重复通知；合成基准中 500 节点批次筛选耗时降低约 91%，实际收益随数据规模变化。
- 聊天图片上下文仅解码最近 14 张有效图片，减少历史图片内存分配；原生字符串清理避免不必要的复制。
- 用量统计采用有界分页和一致快照，合并并发缓存填充并回收过期缓存；优化日志深页查询的索引范围。
- 素材批量清理复用画布引用读取并去重，视频缩略图按需加载；带哈希的静态资源启用长期不可变缓存。

## 问题修复

- 修复工作流模板上传期间切换账号或编辑器造成的误清理、内容串入和遗漏保存。
- 完善工作流、画布和素材的文件引用完整性，以及跨实例、跨账号共享模板的删除保护；直传文件仅删除记录时保留远端文件。
- 修复素材列表延迟返回覆盖期间成功变更、素材选择器关闭重开后旧排队请求继续写入的问题。
- 修复批量删除部分失败后遗漏清理的问题：继续处理其他项，仅清理成功项并保留失败项的选择。
- 修复视频预览未加载、浏览器 Storage 异常影响操作，以及审计逻辑吞掉请求体读取错误的问题；已知过大的请求体不再提前读取。
- 修复 SSO 初始化时的身份读取竞争，并按 New API 的 50 字节限制截断自动创建的 Key 名称。
- 修复 pre-commit 钩子将部分暂存 Go 文件的未暂存修改带入提交的问题，并补充相关回归测试。

## 移除与调整

- SSO 自动创建分组 Key 需要平台端同步更新并提供 `/api/sso/chatgpt2api/tokens` 接口，同时仍需配置上游数据库连接。
- 移除外部 Google Fonts 请求和全局强制替换深色颜色的样式规则，统一使用本地字体栈和主题样式。
