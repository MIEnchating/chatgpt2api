# v3.0.6

## 版本概览

改进视频模型契约文档导入，支持从多页面文档站完整发现、筛选并生成视频契约。

## 新增功能

- 支持通过 `llms.txt`、HTML 导航链接及 Swagger/Redoc OpenAPI 引用发现同站文档页面。
- 增加 AI 文档阅读规划阶段，从站点候选中选择视频创建、查询、状态及必要 Schema 页面。
- 视频契约请求字段支持数组下标路径，例如 `images[0]` 与 `images[1]`。
- 视频时长字段支持显式配置为 JSON 数字或字符串类型。

## 功能改进

- 文档导入上限提高到 180,000 个字符，并显示文档规划、相关页面读取和生成修复进度。
- 契约生成可一次汇总多份契约的校验错误，并增加一次自动修复机会。
- 生成提示与 JSON Schema 对齐素材数量、模式选择、固定时长和请求字段约束。

## 问题修复

- 修复 Apifox 等站点通过内容协商返回单页 Markdown 时，只生成入口页两份契约的问题。
- 修复规划阶段遗漏入口协议对应候选页面的问题。
- 修复纯图生视频契约被强制要求文生视频默认模式的问题。
- 修复固定时长且禁止发送时长字段的模型无法通过契约校验的问题。
- 修复首尾帧共用数组字段时无法正确构造请求体的问题。
- 修复生成 Schema 允许的素材上限与后端业务校验不一致的问题。

## 移除与调整

- 视频契约存储格式升级至版本 8，已有契约会补充时长值类型。
