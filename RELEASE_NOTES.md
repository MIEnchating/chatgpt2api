# v3.0.1

## 版本概览

本版本集中完善视频模型契约的生成、审核与批量管理流程，并修复远程开发访问、页面切换重载、视频任务重试和异步状态覆盖问题。契约文档分析现在会依据真实协议差异生成多份独立草稿，生成过程可实时查看、停止和重试。

## 新增功能

- 视频契约文档分析支持输出多份契约草稿，按模型协议、能力和字段差异自动拆分，相同协议的模型仍可安全合并。
- 文档分析新增流式进度展示，实时呈现文档读取、上游连接、内容接收、字段校验、自动修正和请求重试状态，并支持主动停止。
- 视频模型契约列表新增跨分页多选、当前页全选、批量启用、批量停用、批量导出和带二次确认的批量删除。
- 模型配置支持同时选择多个普通或自定义 Key 获取模型，并对多个上游返回的模型列表去重合并。

## 功能改进

- 文档生成改用严格结构化输出与契约集合校验，限制模型必须来自原始文档，并在首次结果不合法时携带具体错误自动修正。
- 契约草稿审核支持逐份发布、编辑或移除，发布后继续处理下一份，同时保留冲突提示和未审核草稿。
- 批量操作在卡片头部原位替换普通操作，选择状态变化不会挤压表头或导致列表位置跳动。
- 视频契约列表采用稳定分页和独立滚动区域，异步加载、版本历史与并发修改使用会话和请求版本隔离。
- 视频生成失败后重试或整轮重新生成时使用当前选中的模型，并根据新模型重新校验和归一化参数。
- 全局悬浮消息新增关闭按钮，图片预览和通用弹窗的关闭按钮避开滚动条并保持固定位置。

## 问题修复

- 修复切换页面或窗口重新获得焦点时认证检查触发整页加载、路由退场动画导致页面反复卸载的问题。
- 修复远程访问前端时 Vite WebSocket 连接失败、依赖资源响应长度不一致和开发服务不稳定的问题。
- 修复视频契约文档包含多种协议时错误合并为单份契约，以及模型臆造、条件规则结构不一致和响应过长导致生成失败的问题。
- 修复契约生成期间只能等待转圈、上游超时缺少明确反馈、旧请求响应覆盖新会话状态的问题。
- 修复切换视频模型后点击重试仍沿用原任务旧模型及旧参数的问题。
- 修复批量契约操作的完整列表响应可能乱序覆盖、部分失败后选择状态与服务端结果不一致的问题。

## 移除与调整

- 移除页面路由切换的退场动画，页面内容不再因导航动画被重复卸载和挂载。
- 文档生成接口调整为 NDJSON 进度流，并返回契约集合；管理端同步使用当前接口，不保留旧的单契约响应路径。
- 移除已被现有会话生命周期测试覆盖的旧生成结果会话守卫静态测试。
