# 2026-09-14 全局续审

本记录接续 [同日审查记录](2026-09-14.md)，以本轮修复及各项验证时的源码快照为准。保留开始时已有的未提交修复和功能改动；本次新增问题集中在引用完整性、文件记录删除和浏览器存储异常。修改尚未提交或发布。

## 新增发现与修复

| 编号 | 级别 | 问题 | 修复 |
| --- | --- | --- | --- |
| F07 | P2 | 工作流模板同时包含 storageKey 和本机 URL 时，只检查 storageKey 指向的文件；URL 指向的文件即使不存在或正在删除仍可保存，后续删除保护也会漏查它。 | 保存、初始化和删除保护统一检查两处引用的全部对象 ID，并去重。 |
| F08 | P2 | 共享模板被另一账号的工作流引用时，文件 owner 的删除检查漏掉引用；另一账号还能在删除期间保存引用。 | 删除保护遍历全部工作流，保存和初始化检查全部 owner 的待删除集合；CAS 重试重新验证。跨 owner 错误不披露工作流名称。 |
| F09 | P2 | DELETE /api/files/:id/record 绕过素材、画布和工作流引用保护，直传文件记录删除后留下失效引用。 | 记录删除通过素材服务的持久删除队列和引用协调器；持久保存只删除记录的操作意图，恢复和重试不删除远端文件。 |
| F10 | P2 | 当前浏览器因站点策略禁止 Storage 访问或空间耗尽而抛错时，创作台初始化/偏好写入和画布面板写入会抛出未处理异常；提示词套用缺少可见的失败反馈。 | 可选界面偏好读写处理当前 API 的异常；提示词暂存明确返回成功与否，失败显示错误且不跳转。 |

未保留上一代理新增的 IntersectionObserver 缺失兼容分支；遵循仓库不添加兼容层的约束。Storage 异常处理使用现有 API，不引入替代持久化机制。

## 验证

| 检查 | 当前结果 |
| --- | --- |
| `npm test`（web） | 811 项通过，117 个测试文件，0 失败 |
| `npm run build`（web） | 通过 |
| `npm run lint`（web） | 0 错误、0 警告 |
| `go test ./...` | 9 个测试包通过，`internal/model` 无测试 |
| `go vet ./...` | 通过 |
| `go test -race -p 1 -timeout=10m ./...` | HTTP 包达到 600 秒超时；后续包执行因运行环境重启中断，不计为通过 |
| 完整清单分组 race 检查 | 通过：HTTP 359 个顶层测试分 4 组，其余 8 个含测试包通过；无数据竞争报告 |
| `go build -tags=embed -o /tmp/chatgpt2api-review-followup-embed ./internal` | 通过 |
| Git hook 和发布说明校验 | 5 个 hook 场景通过；发布说明校验通过 |
| 修改文件 gofmt 与 `git diff --check` | 通过 |
| 真实源码服务 HTTP 验证 | 26 项断言通过 |
| Chrome 桌面/手机版面与行为 | 媒体/工作流回归通过；8 组 Storage 故障注入通过；无 pageerror |

F07/F08 的新增 Go 测试在旧实现下复现 11 处错误放行；F09 的 HTTP 复现为在用记录删除返回 200、后续查询 404，修复后对应状态为 409/200。新增服务测试覆盖画布和工作流引用、跨 SQLite 实例/重启恢复、分组修改保留删除模式及远端文件保留。

最终竞态验证使用当前源码编译的 HTTP race 测试程序，按实际列举的 359 个顶层测试分为 90/90/90/89 四组，以两个并发进程执行，每组超时 20 分钟。清单并集完整且无重复；四组均退出 0，耗时约 149/197/181/183 秒。其余包运行 `GOMAXPROCS=2 go test -race -p 1 -timeout=20m ./internal ./internal/config ./internal/model ./internal/protocol ./internal/service ./internal/storage ./internal/util ./internal/videocontract ./internal/web`，退出 0，服务包耗时约 186 秒。该结论来自完整清单分组执行，不代表此前超时并中断的全量命令通过。清单、各组真实退出状态与日志保存在 `build/review/2026-09-14-followup-http-race-*`，其余包日志为 `2026-09-14-followup-race-other-packages.log`。

浏览器在 1440x1000 和 390x844 下检查提示词失败反馈、创作页编辑、画布挂载和侧栏切换。另重跑媒体/工作流回归，延迟视频进入视口后解码正常，视频像素抽样为 `[238, 40, 70, 255]`。本地证据保存在忽略目录 `build/review/`：`2026-09-14-browser-storage-results.json`、`2026-09-14-browser-results.json`、`2026-09-14-frontend-finalize-*`、`2026-09-14-followup-go-race.log` 和 `2026-09-14-runtime/smoke-results.json`。

上述浏览器和 HTTP 验证使用 Vite `http://127.0.0.1:8003` 与隔离的 `go run` 后端 `http://127.0.0.1:8091`。这些隔离进程随后因运行环境重启结束。收尾时确认默认源码服务 Vite `http://127.0.0.1:8002` 与 `go run` 后端 `http://127.0.0.1:8090` 正在运行，前端页面及两端 `/health` 均返回 200。隔离数据与临时测试账号信息在本地 `build/review/2026-09-14-runtime/`，本次修复未修改真实 `.env`、业务数据与备份。

## 审查范围

复核了前后端当前改动及高风险调用关系，包括配置、认证与权限、relay 凭据使用、数据库 CAS、任务持久化、素材与工作流删除、出站请求限制、前端异步生命周期和浏览器持久化，以及 CI、发布脚本和 Git hook。通过全仓库测试和静态检查验证各模块的集成，不把历史清单的哈希当作当前版本逐行审查证明。

竞态测试运行期间，工作区另有并行的 SSO 改动：`internal/service/relay_onboarding.go`、对应服务测试及新建的 `internal/httpapi/sso_onboarding_test.go` 在测试程序编译后更新。这些后续改动保留原样，未包含在上述竞态验证结论中；本轮 F01-F10 修复文件在收尾期间未发生其他代码修改。

本轮没有真实调用 ChatGPT、relay、OAuth/SSO 或远端 S3/WebDAV，也未修改上游协议；数据库行为回归使用隔离 SQLite。PostgreSQL/MySQL 的破坏性集成测试需要显式测试数据库配置，默认验证不执行它们。浏览器验证使用本地合成数据与 mock 接口，不代表真实上游成功。
