# 2026-09-14 全局审查与修复

审查起点为 `c3c2e46`。结合 2026-09-07 的历史审查记录，检查 `2599571..c3c2e46` 的 97 个变更文件（新增 3446 行、删除 338 行），并沿当前代码复核鉴权、存储、任务、媒体和前端异步生命周期等高风险调用路径。本轮确认并修复以下 6 个问题，修改尚未提交或发布。

## 已修复问题

| 编号 | 级别 | 触发场景与影响 | 修复 |
| --- | --- | --- | --- |
| F01 | P1 | 工作流模板素材登记未完成时切换账号，旧请求失败后会使用新会话删除旧账号上传的文件；新会话有管理权限时可能实际删除。 | 清理前核对发起上传的会话，并为上传传入页面生命周期的取消信号。 |
| F02 | P2 | 模板上传期间关闭编辑器，再打开其他工作流或重新打开同一个工作流，晚到结果会附加到新编辑器。 | 为编辑器每次打开、关闭和替换维护代次；过期结果不附加，未登记的临时文件回收，已登记素材保留在素材库。 |
| F03 | P2 | 模板上传尚未结束时可以保存工作流，保存成功却遗漏正在上传的模板。 | 保存按钮在上传期间禁用，保存函数同时检查同步上传计数。 |
| F04 | P2 | 工作流文件删除保护仅存在内存中，其他实例、重启后的实例和初始化接口可在删除期间写入引用；删除完成后旧草稿也能保存失效引用。 | 删除状态与工作流引用保存在同一 CAS 文档中；保存与初始化每次重试均检查删除状态和本机对象是否存在，其他文档写操作保留删除状态。 |
| F05 | P2 | 素材列表中非首批视频使用 `preload=none`，滚动进入视口后仍不加载画面。 | 使用 IntersectionObserver 在进入视口时设置视频源并加载 metadata，卸载时断开观察，媒体源改变后重新建立可见性状态。 |
| F06 | P2 | pre-commit 对部分暂存的 Go 文件执行 `gofmt -w` 和 `git add`，将用户未暂存的修改意外带入提交。 | 部分暂存文件仅检查 index 中的格式；需要格式化时明确失败且保持 index 和工作区内容，完整暂存文件继续自动格式化。 |

修复位置：

- F01-F03：`web/src/app/workflows/creative-workflow-workspace.tsx`。
- F04：`internal/service/workflow.go`。
- F05：`web/src/app/assets/asset-display.tsx`。
- F06：`.githooks/pre-commit`。

## 回归证据

用于复现缺陷的回归用例在修复前确认失败，修复后通过。前端回归直接执行从实际组件提取的函数或编译后的组件；浏览器另行验证真实 React 行为。

- `web/tests/workflow-template-upload.test.mjs`：切换及重新打开编辑器、登记期间切换编辑器、登记失败时切换账号、正常模板上传、上传期间保存保护，共 6 项。
- `web/tests/asset-video-thumbnail.test.mjs`：进入视口加载、首批立即加载、卸载清理、媒体源改变，共 4 项。
- `internal/service/workflow_deletion_test.go`：使用同一 SQLite 数据库的独立后端实例，覆盖保存/初始化、服务重建、CAS 交错、其他文档写操作、删除完成后的旧引用、错误分类及历史失效引用修复。
- `scripts/test-git-hooks.sh`：在临时 Git 仓库中验证完整暂存、部分暂存、含空格文件名、格式错误和语法错误，共 5 个场景。
- Playwright 在 1440x1000 和 390x844 下验证延迟视频加载、旧上传结果拒绝、上传期间保存禁用及正常上传。第 12 项视频进入视口后已解码，抽样像素为 `[238, 40, 70, 255]`，两个视口均无页面异常。接口和媒体使用本地合成数据。
- 隔离 `go run` 服务通过实际 HTTP 验证：上传/保存返回 200，在用文件删除返回 409 且文件仍可读取，移除引用后删除返回 200、查询返回 404，使用旧 storageKey 或本机 URL 保存及初始化均返回 400，拒绝操作没有持久化工作流。共 17 项检查通过，结果见 `build/review/2026-09-14-runtime/smoke-results.json`。

## 验证结果

| 检查 | 结果 |
| --- | --- |
| `npm test`（web） | 799 项通过，115 个测试文件，0 失败 |
| `npm run build`（web） | 通过 |
| `npm run lint`（web） | 0 错误、0 警告 |
| `go test ./...` | 9 个含测试包通过，`internal/model` 无测试 |
| `go vet ./...` | 通过 |
| 全包 race 检查 | 通过；HTTP 357 个顶层测试分 4 组执行，其余包单独执行 |
| `go build -tags=embed -o /tmp/chatgpt2api-review-embed ./internal` | 通过 |
| `bash scripts/test-git-hooks.sh` | 5 个场景通过 |
| `bash scripts/validate-release-notes.sh` | 通过 |
| 桌面及手机浏览器专项检查 | 通过 |
| 隔离源码服务 HTTP 检查 | 17 项通过 |
| Vite 界面真实登录隔离后端 | 会话验证返回 200，无页面异常 |
| `git diff --check` 及修改文件 gofmt 检查 | 通过 |

本地日志、浏览器脚本、合成媒体截图和结果在被 Git 忽略的 `build/review/2026-09-14-*` 下；最终构建及测试日志使用 `2026-09-14-final-*` 前缀。这些产物不包含真实业务凭据或真实上游响应。

两次直接运行 `go test -race ./...` 均被 SIGTERM 中断，没有获得完整退出结果；这两次不计为通过。随后改为后台监督执行，将 HTTP 包 `go test -list` 的 357 个顶层测试按 90/89/89/89 分组，分别运行 `go test -race -timeout=20m ./internal/httpapi -run <精确正则>`，其他包运行相同 race 选项。清单校验确认并集完整且无重复，5 个任务最终全部退出 0，4 组 HTTP 测试分别用时约 114/165/144/141 秒。覆盖清单、命令及真实退出状态见 `2026-09-14-final-go-race-groups-manifest.json` 和各组 `.status.json`。该验证覆盖完整测试清单，但没有把分组执行描述为原始全量命令正常退出。

## 范围与限制

本轮复核覆盖当前变更及相关调用链：登录/SSO 与权限、配置和 relay 分组、存储 CAS 与素材删除、工作流模板持久化、创作任务结果处理、媒体显示、前端会话与异步状态、构建发布及 Git hooks。旧审查报告用于定位历史风险；本轮没有重新逐行审计所有未变更源码或全部第三方依赖，没有把旧 `inventory.json` 刷新为本轮逐文件完成证明。

真实 ChatGPT/relay 上游、OAuth/SSO 提供方以及真实 S3/WebDAV 未联调；本轮数据库竞态验证使用 SQLite，未重跑真实 PostgreSQL/MySQL 集成。没有修改上游协议行为，未依据旧 jshook 记录推断新的协议。测试通过不代表已穷举所有部署环境或保证没有其他缺陷。

开发验证使用 Vite 源码模式和 `go run ./internal`，端口为 8003/8091，避开已有 8002/8090 服务。后端使用隔离的数据目录和临时测试账号。真实 `.env`、原有业务数据和 `chatgpt2api.previous` 保留不动。
