# 全局审查记录

审查日期：2026-09-07。范围为当前工作区的应用源码、配置、构建发布流程和相关测试。

当前状态：**约定范围内的逐文件适用审查已完成**。最终清单包含 594 个文件，全部 reviewed，当前文件 SHA256、字节数、适用的行数及 Git 文件覆盖校验通过。涵盖应用源码、测试、文档、研究脚本、构建部署配置和提交的静态资源；真实 `.env`、业务数据、安装依赖目录、生成构建产物和原有备份 `chatgpt2api.previous` 不属于本次源码审查范围。审查产物在 `review/` 单独记录。

源码采用完整阅读与调用关系检查，修改后复核差异；二进制图片/模型采用解码、结构和可视检查；提交的第三方静态 JS 包采用完整解析、业务代码审阅及浏览器回归，**没有声称逐行审计其中所有第三方库实现**。文件覆盖完整不意味着穷举所有运行环境或保证不存在其他缺陷。最终权威清单是 `review/inventory.json`，可运行 `python3 review/validate-inventory.py` 重验；分批结果文件保留各阶段快照。

最终验证：前端 773 项测试、build、lint；Go 全量测试、vet、全库 race、embed 构建；6 项研究脚本离线测试及发布说明格式校验均通过。重新构建的隔离服务完成桌面/手机各 11 条路由、画布保存/冲突/退出检查。审查完成时修复尚未提交或发布，后续发布由版本流程执行；临时验证服务已停止。

## 覆盖进度

| 模块 | 检查范围 | 当前状态 |
| --- | --- | --- |
| 启动与部署 | main、config 环境读写、Dockerfile、Compose、CI、发布脚本 | 已检查；R02/R11；Compose 临时容器验证 |
| 身份与权限 | auth、password_accounts、permissions、newapi_tokens、login_limiter | 已检查；R05/R08/R10/R14；双实例 SQLite 回归及独立子代理复核 |
| HTTP 边界 | router/routes、requireIdentity、JSON/multipart、CORS、错误映射、audit | 已检查；R01/R10/R13；路由及上传边界回归 |
| 持久化 | storage.go 行/文档 CAS、storage_objects、image_conversations 事务/游标 | 已检查；R02；真实 PostgreSQL/MySQL 临时库集成通过 |
| 图片与媒体存储 | generic_storage/local/webdav、image、media_proxy；所有权、取消、范围下载、容量与清理 | 已检查；R01/R03；外部 Provider 见验证边界 |
| 会话历史与附件 | image_conversation_history/rows/assets；批量 CAS、清空代次、引用、上传回滚、写入限流 | 已检查；服务/路由/真实数据库回归 |
| 创作任务 | image_task 提交/幂等、配额、取消、合并、删除标记、恢复、停机 | 已检查；R09；常驻实例删除及 ID 分隔符回归 |
| 上游转发 | relay/audio/image_api_modes、core、代理、SSE 解码与错误、媒体下载凭据边界 | 已检查本地实现；R06；真实上游未联调 |
| 视频模型契约 | videocontract/store、protocol 契约校验、HTTP 导入/文档读取/发布、运行时刷新 | 已完成适用检查；既有契约/适配测试；各供应商未真实执行 |
| 画布与工作流后端 | canvas_document 修订/分组/资源删除围栏；workflow 保存冲突、Agent 草稿解析 | 已检查；R07；画布保存及 409 浏览器验证 |
| 配置与管理业务 | 设置装配、announcement、preferences、prompt_favorite、my_asset 清理事务、log 分页/脱敏 | 已检查；R11/R13；管理与存储失败回归 |
| 前端公共层 | session、request、权限守卫、请求缓存、mutation lifecycle、媒体存储与结果登记 | 已完成适用检查；R04/R10；前端测试 |
| 创作台 | image/page 的请求/恢复路径、video task queue/state、history store、参考素材与任务结果 | 已完成适用检查；桌面/手机浏览；生成结果用模拟回归 |
| 画布与 Agent | canvas-save/groups/history、project-transfer、task queue、Agent run gate/runtime、director 消息来源 | 已完成适用检查；R12；归档真实 ZIP 单测与页面检查 |
| 工作流前端 | workflow-runtime/task-runtime、reference-lifecycle、agent-draft-lifecycle、workspace 提交/恢复 | 已完成适用检查；既有生命周期回归与页面检查 |
| 管理页面 | 登录、用户、角色、settings store、个人资料、日志、素材、提示词的入口/会话隔离 | 已完成入口与请求生命周期检查；R10；全部主路由桌面/手机浏览 |
| 浏览器与回归验证 | 1440x1000 与 390x844；11 个主路由、登录/退出、保存/冲突 | 浏览器与最终 Go 全量测试通过；race 结果见验证证据 |

## 已确认问题

| 编号 | 级别 | 问题 | 修复与证据 |
| --- | --- | --- | --- |
| R01 | P1 | 图片代理返回同源 SVG 缺少脚本隔离 | 增加 CSP；HTTP 回归及真实 Chrome 对照验证 |
| R02 | P2 | SQLite 连接重建后丢失初始化参数 | 改由 DSN 在每次连接时应用；重连回归 |
| R03 | P2 | 取消的本地上传仍保存并返回成功 | 读取和提交前检查取消，回滚未提交文件；服务回归 |
| R04 | P2 | 素材登记失败丢失已上传文件信息，重试重复上传 | 保留上传结果；失败再重试回归 |
| R05 | P1 | 外部用户退出后丢失禁用与自定义角色，重登可恢复默认权限 | 过期会话保留管理状态；禁用用户登录返回 403；跨重启回归 |
| R06 | P2 | SOCKS5 握手阻塞时取消 context 无法中止连接 | 取消回调关闭连接，成功交接前注销回调；停滞握手复现/回归 |
| R07 | P2 | 工作流 Agent 返回 JSON null 时对 nil map 写入而 panic | 拒绝空对象；两种 null 输入复现/回归 |
| R08 | P1 | 多实例登录、退出和权限变化不被另一实例及时识别 | 鉴权/登录/退出刷新持久化状态，避免旧角色覆盖新会话；双实例回归 |
| R09 | P2 | 常驻实例重新合并已删除任务；含冒号 ID 的删除标记拆分错误；删除后重用 ID 产生不可查询任务 | 合并过滤删除标记，内部键用无歧义分隔符，拒绝重用仍在保留期的已删除 ID；回归 |
| R10 | P2 | 密码首尾空格被修改，且长度校验超过 bcrypt 上限 | HTTP/初始管理员保留原文，前后端统一 8-72 UTF-8 字节；登录与边界回归 |
| R11 | P2 | 设置中含美元符号的值在 Compose 重建时被插值改写 | 写入双美元符号，读取还原；Go 往返测试及 Compose 实际容器环境对照 |
| R12 | P2 | 画布归档静默遗漏媒体、重复上传共享文件，导入后嵌套 URL 指向旧对象 | 导出缺失时报错，导入先验证文件/项目数，去重并重写引用；上传失败清理；真实 ZIP 回归 |
| R13 | P1 | 日志保存数据库/代理 URL 的用户名密码及媒体签名参数 | URL 用户信息与查询参数脱敏，覆盖字段后缀及字符串数组；日志保存回归 |
| R14 | P1 | 默认角色撤权与登录交错时，旧权限被重新签发并持久化 | 默认角色也纳入角色文档 CAS；密码与外部登录确定性交错复现，并检查角色分配路径 |

## 逐文件续审新增问题

| 编号 | 级别 | 问题 | 修复与证据 |
| --- | --- | --- | --- |
| R15 | P1 | 用户显式 local provider 可写入或测量任意服务器目录 | 用户 provider 仅接受 S3/WebDAV；服务及 HTTP 回归 |
| R16 | P1 | 研究脚本跨域下载复用认证 Session，捕获可能覆盖提交样本 | 同源认证/跨域无凭据下载，私有独立捕获，6 项离线测试 |
| R17 | P2 | Responses 部分预览或失败状态被返回为成功图片 | 只接受最终输出并拒绝 failed/incomplete；SSE 复现与回归 |
| R18 | P2 | 配置启用 Debug 后日志仍被默认 Info 阈值丢弃 | slog handler 允许 Debug，再使用已有配置过滤；回归 |
| R19 | P2 | 外部数据库 schema 探测误命中其他库/Schema 的表列 | 当前数据库/实际关系查询；临时 PostgreSQL/MySQL 回归 |
| R20 | P2 | 公开素材可以列出却不能跨用户读取本机对象 | 素材服务以对象真实所有者的公开引用授权 Info/GET/HEAD，保留删除权限边界；撤销/伪造/匿名回归 |
| R21 | P1 | 私有图库缩略图允许共享缓存保存一年，公开撤销也无法及时生效 | 原图/参考图/缩略图设 private no-store；可见性切换回归。已存在的外部缓存副本无法由本地补丁追溯清除 |
| R22 | P2 | 账户切换后的旧模型响应覆盖当前视频合同 | 配置失效代次检查；修改前失败、后通过 |
| R23 | P2 | 清空图片缓存后，旧组件释放操作减少新缓存引用 | 缓存键加入代次，避免跨代释放；修改前失败、后通过 |
| R24 | P2 | 前端 IPv4-mapped IPv6 标准化后绕过私网 URL 校验 | 解析十六进制映射 IPv4 再验证；前端回归，后端出站保护独立 |
| R25 | P2 | 通用 URL 收集器提前脱敏，破坏音频结果签名链接 | 业务值保留签名，日志持久化时脱敏；结果保真与日志脱敏同一回归 |
| R26 | P1 | 数据库诊断 URL 的查询参数或片段泄露密码 | 删除 query/fragment，非法 URL 脱敏；存储专项与 race |
| R27 | P2 | 数据库密码更新/重载裁剪首尾空白 | relay_database_password 保留原文；配置往返专项与 race |
| R28 | P2 | 视频结果字段路径忽略大小写去重，丢失不同 JSON 键 | 字段路径按大小写敏感规则去重；协议及 HTTP 合同回归 |
| R29 | P2 | 图片宽高比接受 NaN/Inf 或除法溢出/下溢 | 要求尺寸与计算比例有限且为正；数值边界回归 |
| R30 | P2 | 缺失日志时间被保存为 <nil>，正文与 SQL 时间不一致 | 生成默认时间后再编码正文；存储专项与 race |
| R31 | P2 | 11–15 张生成批次无法保存画布 | 生成数量及批次子节点统一上限 15；15 张保存重载/16 张拒绝回归，修复前失败后通过 |


| R32 | P1 | 图片历史批量重试等待期间切换账号，旧快照仍使用新会话提交 | 每次重试前重新核对会话；确定性等待/切换回归，修复前发出 2 次、后仅 1 次请求 |

| R33 | P2 | 工作流系列规划晚到，旧提示词覆盖新打开的工作流 | 按 runner 代次与账号检查提交 UI/自动生成；真实 Chrome 修复前 staleCount=1、后=0，同一 runner 正常返回=1 |
| R34 | P2 | 视频任务重载丢失超时配置，其他实例按默认值误判中断 | 重载保留私有 timeout 字段；任务恢复回归 |
| R35 | P2 | 图像任务重载遗漏 stream/partial_images/moderation 选项 | 复用图像工具字段规范化；持久化重载回归 |
| R36 | P1 | 其他实例已删除任务，热实例仍以缓存授予媒体权限 | 授权前刷新持久化任务状态；跨实例删除回归 |
| R37 | P2 | 引用图片元数据已提交但返回错误时误删已提交文件 | 失败后回查元数据，不能证明未提交则保留文件；不确定提交回归 |
| R38 | P2 | 引用素材过期清理后，同一历史会话无法继续保存 | 保留属于当前 owner 的规范引用元数据；文件访问仍拒绝缺失对象；清理后保存及 owner 边界回归 |

| R39 | P2 | relay SSE 完成后继续读取尾部，导致成功结果误报失败或等待关闭 | [DONE] 后终止流并关闭 body；完成后读错误/尾部错误回归 |
| R40 | P2 | 空视频保存成功并覆盖原有产物 | 零字节拒绝提交，清理临时文件；原文件保留回归 |
| R41 | P2 | relay 图片比例 NaN/Inf 或大数运算溢出 | 有限比例校验、先除后乘及避免整数舍入溢出；极限尺寸与正常宽高方向回归 |

R39–R41：`build/review/http-remaining-tests.log` 与 `http-remaining-race.log`，HTTP 全包及 relay race 通过。

R33 浏览器证据：`build/review/workflow-draft-race.mjs`、`workflow-draft-before.log`、`workflow-draft-after.log`、`workflow-draft-current.log`，使用本地 Vite 和模拟任务响应，未调用真实上游。R34–R38 证据见 `review/service-image-results.json` 与 `build/review/service-image-final-tests.log`。

R32 专项：`build/review/history-retry-before.log`、`history-retry-after.log`。

R26–R30 增量证据：`build/review/backend-foundation-tests.log`、`backend-foundation-race.log`、`backend-foundation-boundaries.log`。R31：`canvas-batch-before.log`、`canvas-batch-after.log`。

R15–R25 批次证据：`build/review/round2-go-all.log`、`round2-vet.log`、`round2-embed-build.log`、`web-lib-all-tests.log`、`web-lib-build.log`、`web-lib-lint.log`，以及各问题的 before/after 和服务专项日志。后端全量测试、vet、embed 构建通过；前端 698 项测试、build、lint 通过。逐文件完成不等于已穷举全部运行环境；源码、测试、资源的适用检查方式分别记入文件记录。


R42–R49 画布续审已完成，源文件和测试逐项记录见 `review/web-canvas-results.json`、`review/web-canvas-test-results.json`。

| 编号 | 优先级 | 问题 | 修复与验证 |
| --- | --- | --- | --- |
| R42 | P2 | 部分成功批次恢复时输出索引错位 | 保留完整任务输出顺序，仅更新待恢复节点；行为回归 |
| R43 | P2 | 切换批次主图仍指向旧存储对象 | 同步存储标识、媒体类型和大小；主图切换回归 |
| R44 | P1 | 复制批次保留原节点引用，删除副本会误删原图 | 重映射批次引用，复制包含子图，单节点复制解除归属；复制删除回归 |
| R45 | P2 | 撤销或切换画布后旧任务结果污染节点与历史 | 各生成路径检查画布与操作代次；晚到成功/失败回归 |
| R46 | P2 | 停止文本生成后仍加载并错误提示失败 | 取消后统一收尾，阻止成功路径继续提交；取消回归 |
| R47 | P2 | Agent 取消竞态与已取消任务残留队列 | 提交前后检查取消并保留 AbortError；取消任务退出 pending |
| R48 | P2 | 输入法确认误选画布引用、误提交图片生成 | 组合输入期间忽略快捷键；实际键盘处理函数回归 |
| R49 | P2 | 画布批量删除后弹窗未关闭，部分失败会重复删除 | 逐项移除成功记录，保留待删数量，成功关闭弹窗；部分失败回归 |
| R50 | P2 | 图片编辑更换模型仍选用旧模型的 Key | 使用编辑草稿模型选择 Key；修复前后回归 |
| R51 | P2 | 单张重试残留上次产物存储标识 | 新任务清除 storageKey；修复前后回归 |
| R52 | P2 | 草稿替换后旧视频/音频上传晚到污染草稿 | 上传及音频解析后检查草稿代次和页面生命周期；修复前后回归 |

R48 图片工作台与 R50–R52 证据：`build/review/studio-before.log`、`studio-audio-before.log`、`studio-ime-before.log`、`studio-after.log`；七项实际回调执行回归通过。尚不代表后续全部前端更改的全量验证完成。

## 阶段验证说明

前述 round2/round3、各子代理 tests/build/lint 和 before/after 记录属于增量阶段；最终合并版本结果以下节为准。中间出现过测试夹具未随函数签名更新、素材表单 Promise 类型联动及 effect 依赖检查失败，均已修正并在最终全量验证中通过。

## 验证证据

- 前端最终：`final-complete-web-tests.log`，773 tests、0 failures；`final-complete-web-build.log` 与 `final-complete-web-lint.log` 通过，0 lint warnings/errors。
- Go 最终：`final-complete-go-tests.log`、`final-complete-go-vet.log`、`final-complete-go-race.log`、`final-complete-embed.log` 全部通过。全库 race 包括 HTTP 与 service，未报告数据竞争；HTTP race 耗时约 581 秒。
- 其他最终检查：`final-complete-jshook-tests.log` 6 项离线测试通过；`final-complete-release-notes.log` 格式通过；`final-inventory-validation.log` 验证全部 594 个当前文件与覆盖范围；`git diff --check` 通过。
- 浏览器最终：`final-complete-browser.log` 及 `browser/results.json`，1440×1000 / 390×844，各 11 条主路由均 HTTP 200 且无 pageerror；画布保存 200、旧 revision 409、退出后访问 401。隔离 SQLite 数据和演示凭据只在 `build/review/final-browser-state/`，未使用现有业务数据。
- 专项浏览器：`management-invalidation-browser.log` 验证素材/提示词/设置同账号失效后重新验证可恢复；工作流旧草稿、跨账号分块、同账号正常生成、恢复后发现新子任务与过期缺失均通过。导演台桌面/手机 GLB 加载、删除最后机位、撤销、截图发送与关闭握手见 `static-director/`。
- 数据库集成阶段：独立 `postgres:18-alpine` 与 `mysql:8.0` 容器中的集成回归通过；没有连接现有业务数据库。相关服务/存储实现随后通过最终 Go 全量与 race。
- 安全扫描阶段：govulncheck 未发现可达漏洞；另有 2 个已导入包、19 个依赖模块中的漏洞，扫描未发现代码触达其漏洞符号。不能据此称依赖无漏洞。
- 上述日志、脚本与截图路径均相对于 `build/review/`，该目录已被 Git 忽略。早期 `/tmp` 证据已失效，不作为可交付路径。独立子代理结果及当前逐文件清单保存在 `review/`。

## 验证边界

- 真实上游账号登录、生图、视频、音频、模型 Tool Calling 与供应商故障恢复未联调。没有根据旧 jshook 文档修改 ChatGPT 网页协议；模拟响应测试仅证明本地处理逻辑。
- S3/R2/WebDAV 使用代码检查与既有模拟测试，未使用用户的真实凭据执行外部存储操作。多实例本地文件系统容量竞争、网络分区与长时间压力不在本次验证结论内。
- 默认角色现在与自定义角色一样参与会话写入的 CAS；同时登录或管理角色时，共享角色版本冲突的请求可能失败，需要重新发起。回归验证冲突不签发会话，后续新请求使用最新权限；未增加自动重试。
- 浏览器检查覆盖主要路由、基础保存、鉴权和本次修复的专项竞态；未穷举全部参数组合、跨标签页长事务、3D GPU/视频编码组合或 H.264 导出。静态导演台没有上游源码构建工程，重建上游包时必须携带对应业务修复。
- 现有业务数据、真实 `.env` 与用户原有的 `chatgpt2api.previous` 未修改；审查完成时修复尚未提交或发布，后续发布由版本流程执行。

## 共享组件与工作流续审

| 编号 | 优先级 | 问题 | 修复与证据 |
| --- | --- | --- | --- |
| R53 | P1 | 工作流多图生成跨账号继续提交旧提示词 | 账号切换同步取消旧运行器并重建工作区；真实 Chrome 修复前第二张使用新账号，修复后仅首张请求，同账号两张正常提交 |
| R54 | P2 | 滚动区域抢占子输入控件的编辑按键 | 先调用消费者处理器并尊重 defaultPrevented；为原生输入和 ARIA 输入控件保留键盘事件。 新增行为测试在修复前失败；修复后验证输入/编辑/已取消事件不调用 scrollTo，普通滚动键仍正常执行。 |
| R55 | P3 | 只读数字输入仍可通过加减按钮修改 | 只读状态同时禁用步进按钮并阻止步进函数，保留原生 input.readOnly。 新增行为测试修复前触发步进；修复后按钮禁用且不调用 stepUp/stepDown/onValueChange。 |
| R56 | P2 | 多选标签删除按钮无法用键盘激活 | combobox 只处理自身且未被取消的按键，子按钮保留原生键盘激活。 新增行为测试修复前取消嵌套按钮按键；修复后嵌套按钮不被拦截，触发器 Enter 仍打开列表。 |
| R57 | P2 | 卸载或过期队列请求仍触发历史删除 | 在处理响应及删除前统一检查 active 和请求序号。 新增行为测试修复前卸载请求调用 deleteImageConversation；修复后卸载/被取代请求无删除副作用，当前请求仍正常清理。 |
| R58 | P2 | 会话切换复用旧任务队列本地状态 | 以 session.key 作为队列 React key，隔离组件状态与请求生命周期。 新增行为测试模拟会话变更，验证队列 key 从 session-a 切换至 session-b。 |
| R59 | P2 | 浏览器存储不可用阻断任务导航 | 捕获本地记录写入失败，继续派发活动会话事件并导航到创作台。 新增行为测试令 setItem 抛错，确认活动会话事件及 /studio 导航仍执行。 |
| R60 | P2 | 默认登录图片失败后反复重试 | 使用受控 currentSrc 判断是否已切换默认图，交由 React 更新 src，默认图失败后不再赋值重试。 新增行为测试修复前两次手动重设 src；修复后自定义图失败切换默认图，默认图再次失败不触发重试。 |
| R61 | P2 | StrictMode 重放后公告复用失效请求导致不加载 | setup 激活两类生命周期，cleanup 清理所属会话的 in-flight Promise，使重放创建新 token 与请求。 新增行为测试修复前只有一个失效请求；修复后两个请求中只接受新响应并显示最新公告。 |
| R62 | P3 | 首次导航会话验证拒绝未被处理 | 捕获首次加载拒绝，保留由 route guard 统一展示错误和重试。 新增行为测试注入网络拒绝，验证请求已执行且测试进程无未处理 Promise rejection。 |
| R63 | P3 | 普通登录图片预览缺少定位容器 | 所有模式使用 relative 预览框，使计算出的图片尺寸和偏移锚定同一元素。 静态布局检查、TypeScript/Vite 构建与现有布局测试通过；未进行新的浏览器视觉验证。 |

共享组件逐文件记录及验证：`review/web-components-results.json`；R53：`build/review/workflow-session-{before,after,current}.log`。R33 的过期草稿和同工作流正常响应浏览器回归亦再次通过。素材表单保存回调联动及工作流 effect 依赖已修正，`continuation-build.log`、`continuation-lint.log` 通过；后续改动仍需最终全量验证。

## 管理页面续审

| 编号 | 优先级 | 问题 | 修复与证据 |
| --- | --- | --- | --- |
| R64 | P2 | 素材保存失败也显示成功、可重复提交，清空字段残留旧值 | 等待持久化、防重入并显式清空旧字段与媒体元数据；行为回归 |
| R65 | P1 | 素材管理跨账号保留旧工作区，文件清理卸载后继续请求 | 按会话重建内容，串联操作检查取消；清理中止回归 |
| R66 | P2 | 非法 URL fragment 导致个人中心和设置页崩溃 | 捕获无效百分号编码并选择默认节；实际解析函数回归 |
| R67 | P1 | 切换账号期间提示词查重后仍写入素材 | 按会话取消并在写入前检查；等待查重时取消回归 |
| R68 | P2 | 日志治理编辑没有保存入口 | 新增保存日志配置按钮，调用现有 store 保存流程；构建检查 |
| R69 | P2 | 容量统计覆盖新草稿或复活已删 provider，启用操作修改旧对象 | 会话代次及对象身份检查、基于最新配置合并并保持不可变状态；并发回归 |
| R70 | P1 | 视频契约批量操作会话失效后继续请求 | 逐项检查生命周期并阻止过期收尾；批量删除和更新回归 |
| R71 | P3 | 视频契约零帧能力仍预览首帧输入 | 同时校验 frameLimit 大于零；构建检查 |
| R72 | P2 | 调整模型排序后保存的全局默认仍为旧模型 | 四类模型设置同步保存首项默认；实际 store 持久化回归 |

逐文件记录：`review/web-management-results.json`。本批 61 项相关测试、build/lint 通过，11 项新增行为回归。没有新增真实上游或外部存储联调。

## 静态资源、恢复与创作台最终修复

| 编号 | 优先级 | 问题 | 修复与证据 |
| --- | --- | --- | --- |
| R73 | P2 | 历史截图包含账号邮箱、用户 ID 和 token 片段 | 8 张提交截图用不透明样例字段脱敏，API Key 原模糊区改为完全覆盖；解码和可视复核。历史 Git 提交中的旧副本未改写 |
| R74 | P2 | 工作流恢复无法发现后续子任务，部分提交失败后永久运行中 | 完整账号快照轮询；已知子任务均终态且缺失超过恢复预算才失败，保留已有输出。预算为 expectedCount × (timeout + 60 秒)，按最新持久化活动时间起算；正常分块、补齐、仍运行和过期回归及 Chrome 验证 |
| R75 | P1 | 导演台删除最后机位触发 hook 顺序错误而白屏 | 仅在活动机位存在时挂载包含 hooks 的面板；真实 Chrome 删除/撤销回归 |
| R76 | P2 | 导演台粘贴角色/机位改变原机位跟踪目标 | 仅重映射粘贴出的新机位；实际静态包函数两种剪贴板顺序回归 |
| R77 | P3 | bun.lock 工作区名与 package.json 不一致 | 统一当前项目名；完整锁文件解析、包记录结构检查及 frozen dry-run |
| R78 | P1 | 参考图下载结束后可使用新账号上传 | 三个 helper 显式接收原页面会话断言；下载前及上传前验证。队列使用原 session key 与 page epoch，在下载/上传边界校验。 |
| R79 | P1 | 历史恢复保存循环在账号切换后继续保存后续会话 | 每次保存前检查原 session key、页面活动状态、取消状态和 mutation revision；失效后不继续保存、回填或清理其他会话失败状态。 |
| R80 | P1 | 视频提交提前返回导致取消补偿被跳过 | 图片与视频提交共享后置取消检测和补偿；补偿两次请求分别检查原会话，防止延迟中切换账号后继续取消。 |
| R81 | P2 | 关闭 16 倍数对齐后提交、编辑、持久化和队列仍改写尺寸 | 提交和编辑使用开关实际值；精确解析已生成尺寸用于保存/编辑预览；队列保留提交时保存的自定义尺寸，避免偏好后续变化改变旧任务。 |
| R82 | P2 | 排队任务使用当前 API 模式而不是任务快照 | 两条路径均使用 activeTurn.apiMode。 |
| R83 | P2 | 视频重试丢弃正规化后的引用字段 | 完整保存正规化字段，并将仍有效的本地参考图保留为可上传资源、从显式 URL 清单移除重复引用；被新契约隐藏的素材正确清除。 |
| R84 | P2 | 文本回复卡片的重试按钮被处理函数拒绝 | 允许非 chat 的 message 槽位重试；成功图片和 chat 仍不允许单槽重试。 |
| R85 | P2 | 并发上传结束状态、重置和音频时长检查存在竞态 | 使用上传计数/首尾帧 Map，统一重置代次，旧 finally 不影响新代次；音频 inspection 也计入 busy。提交直接检查所有 pending refs。音频时长检查使用同步维护的当前 URL ref，统计已完成并发上传。 |

创作台本批新增 22 项回归，详见 `review/web-studio-followup-results.json`；最终全量已包含这些用例。R65/R67/R70 的会话隔离进一步覆盖 AUTH 事件与 React cleanup 之间的窗口，并以会话代次重新挂载，防止同一 key 重新验证后工作区不能恢复。实际 effect 回归修复前失败、后通过，Chrome 同 key 重新验证通过。

工作流恢复等待期限只处理缺少持久化子任务的历史批次；仍有实际 running 子任务时不会据此判定终止。没有增加或改变上游协议。新增导演台修改均位于业务函数，反向应用两处修改后与原包字节完全一致，第三方库字节保持原样。
