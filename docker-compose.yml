services:
  chatgpt2api:
    build:
      context: .
      dockerfile: Dockerfile
    image: ${DOCKER_IMAGE:-chatgpt2api:local}
    container_name: chatgpt2api
    restart: unless-stopped
    env_file:
      - ./.env
    volumes:
      - ./data:/app/data
      - ./.env:/app/.env
    networks:
      - proxy_network

networks:
  proxy_network:
    external: true
    name: ${DOCKER_NETWORK:-newapi_default}
